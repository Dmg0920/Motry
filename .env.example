# Django configuration
SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=0
# Include your production domain(s) here
ALLOWED_HOSTS=127.0.0.1,localhost,example.yourdomain.com
# Include HTTPS origins for production
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000,https://example.yourdomain.com

# Database (Zeabur provides POSTGRES_CONNECTION_STRING automatically)
# POSTGRES_CONNECTION_STRING=postgresql://user:password@host:port/database

# Deployment
ZBPACK_START_COMMAND=daphne -b 0.0.0.0 -p 8080 config.asgi:application
